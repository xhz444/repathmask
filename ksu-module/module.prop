id=pkgmask
name=PkgMask
version=v1.1.0
versionCode=2
author=PkgMask contributors (kernel core: Andrea-lyz)
description=对指定包名在 /sdcard/Android/{data,obb} 的目录做「不存在」伪装：读操作 ENOENT/隐藏列举，写操作 errno 改写为真实不存在画像（mkdir/创建类 EACCES，unlink/rename 源 ENOENT），含 ino=0 防误伤修复与共享存储四别名匹配，封死 mkdir 探测已装应用的侧信道
